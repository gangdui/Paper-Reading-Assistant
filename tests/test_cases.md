# 测试用例

## TC-01 正常上传 PDF 并生成结构化文献笔记

- 测试目标：验证 PDF 阅读笔记核心链路。
- 输入：一篇可提取文本的 PDF，已配置 `DEEPSEEK_API_KEY`。
- 操作步骤：
  1. 启动项目。
  2. 切换到“阅读 PDF”。
  3. 上传 PDF。
  4. 等待文本提取和清洗完成。
  5. 点击“生成结构化文献笔记”。
- 预期结果：
  - 页面显示结构化文献笔记。
  - 笔记包含 9 个模块。
  - 页面不崩溃。
- 实际结果：
  - 待测试填写。
- 是否通过：
  - 待测试填写。

## TC-02 输入研究方向并成功发现论文

- 测试目标：验证论文发现主流程。
- 输入：`AIGC 模型水印`。
- 操作步骤：
  1. 打开“发现论文”页面。
  2. 输入研究方向。
  3. 点击“扩展关键词并检索论文”。
- 预期结果：
  - 页面展示助手总结。
  - 页面展示 Top 3 优先关注论文。
  - 页面展示完整论文列表。
  - 每篇论文包含标题、年份、数据源、venue、推荐理由、阅读建议和链接。
- 实际结果：
  - 待测试填写。
- 是否通过：
  - 待测试填写。

## TC-03 Semantic Scholar 未配置 Key 时切换到 arXiv

- 测试目标：验证无 Semantic Scholar Key 时仍可运行。
- 输入：`.env` 中不配置 `SEMANTIC_SCHOLAR_API_KEY`，输入研究方向。
- 操作步骤：
  1. 启动项目。
  2. 输入研究方向。
  3. 点击检索。
  4. 如 Semantic Scholar 公开检索失败或限流，观察 fallback。
- 预期结果：
  - 页面不崩溃。
  - 可以切换到 arXiv 预印本数据源。
  - 页面提示 arXiv 结果适合追踪最新论文，正式发表信息需后续核查。
- 实际结果：
  - 待测试填写。
- 是否通过：
  - 待测试填写。

## TC-04 arXiv 失败时进入 mock fallback

- 测试目标：验证真实 API 都不可用时的课堂 demo 稳定性。
- 输入：模拟 Semantic Scholar 和 arXiv 都请求失败。
- 操作步骤：
  1. 在网络不可用或测试环境中触发检索失败。
  2. 查看页面结果。
- 预期结果：
  - 页面显示 mock fallback 示例数据。
  - 页面明确说明 mock fallback 仅用于 demo，不代表真实推荐。
  - 页面不崩溃。
- 实际结果：
  - 待测试填写。
- 是否通过：
  - 待测试填写。

## TC-05 文献核查状态显示正确

- 测试目标：验证文献核查 Agent 的展示结果。
- 输入：论文发现结果。
- 操作步骤：
  1. 完成一次论文检索。
  2. 查看任意论文卡片中的核查摘要。
- 预期结果：
  - 显示来源核查：可追溯 / 待确认。
  - 显示元数据：完整 / 部分完整。
  - 显示质量判断：顶会顶刊 / 正式发表 / arXiv 预印本 / 待进一步确认。
  - 显示“质量判断不等于论文结论可靠”的说明。
- 实际结果：
  - 待测试填写。
- 是否通过：
  - 待测试填写。

## TC-06 英文 abstract 默认折叠

- 测试目标：验证论文卡片不展示过多调试信息。
- 输入：论文发现结果。
- 操作步骤：
  1. 完成论文检索。
  2. 查看完整论文列表。
  3. 查找“更多元数据”折叠区。
- 预期结果：
  - 英文 abstract 默认不直接展开。
  - 用户展开“更多元数据”后可以查看 abstract、作者、命中关键词和 paperId。
- 实际结果：
  - 待测试填写。
- 是否通过：
  - 待测试填写。

## TC-07 Markdown 下载可用

- 测试目标：验证结构化文献笔记可保存。
- 输入：已生成的结构化文献笔记。
- 操作步骤：
  1. 在“阅读 PDF”页面生成文献笔记。
  2. 展开 Markdown 下载区域。
  3. 点击下载按钮。
- 预期结果：
  - 成功下载 `paper_notes.md`。
  - Markdown 内容与页面展示一致。
- 实际结果：
  - 待测试填写。
- 是否通过：
  - 待测试填写。

## TC-08 API Key 缺失时有友好提示

- 测试目标：验证错误提示和页面稳定性。
- 输入：`.env` 中不配置 `DEEPSEEK_API_KEY`。
- 操作步骤：
  1. 启动项目。
  2. 尝试生成 PDF 文献笔记。
- 预期结果：
  - 页面提示缺少 `DEEPSEEK_API_KEY`。
  - 页面不崩溃。
  - 可使用示例输出 fallback 进行 demo。
- 实际结果：
  - 待测试填写。
- 是否通过：
  - 待测试填写。
